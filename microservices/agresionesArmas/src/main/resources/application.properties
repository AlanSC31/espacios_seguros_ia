spring.application.name=agresionesArmas

spring.datasource.url=jdbc:mysql:// sql5.freesqldatabase.com:3306/sql5809114?useSSL=false&serverTimezone=UTC

# Configuracion de la conexion a MySQL
# Reemplaza 'sql5809114' por el nombre real de tu base de datos
spring.datasource.url=jdbc:mysql:// sql5.freesqldatabase.com:3306/sql5809114?useSSL=false&serverTimezone=UTC

# Credenciales de usuario
spring.datasource.username=sql5809114
spring.datasource.password=mFNareUBz5

# Configuracion del driver (opcional en versiones nuevas, pero buena practica)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracion de JPA / Hibernate
# 'update': actualiza el esquema si cambias tus entidades
# 'validate': solo verifica que coincida
# 'create-drop': crea al inicio y borra al cerrar (para pruebas)
spring.jpa.hibernate.ddl-auto=update

# Mostrar las consultas SQL en consola (util para depurar)
spring.jpa.show-sql=true